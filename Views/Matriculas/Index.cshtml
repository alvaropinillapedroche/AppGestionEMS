@model IEnumerable<AppGestionEMS.Models.Matriculas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.alumno.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.curso.promocion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.grupo.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.alumno.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.curso.promocion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.grupo.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fecha)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
